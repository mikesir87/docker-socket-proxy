mutators:
  - type: mountPath
    from: /workspaces/catalog-service-node
    to: /Users/mikesir87/git/dockersamples/catalog-service-node
  - type: addLabels
    labels:
      com.mikesir87.example: test-value
  - type: remapImage
    from: nginx
    to: ghcr.io/mikesir87/nginx-mirror

filters:
  # Remove all items from the lists of containers, images, networks, and volumes that have ALL of the specified labels
  - type: labelFilter
    labels:
      com.example: test-value

gates:
  - type: registry
    registries: ["docker.io", "ghcr.io"]
  - type: namespaceAllowlist
    namespaces: ["library", "mikesir87"]
  # - type: readonly
